# Cursor compatibility entry

Follow `AGENTS.md` for workflow and `docs/CODING_RULES.md` for implementation. This file is an explicit compatibility pointer, not a guarantee that every Cursor version auto-loads this filename. Verify instruction loading in the active client.
